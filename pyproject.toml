[project]
name = "dropzone"
version = "0.1.0"
description = "Dropzone control for Flet"
readme = "README.md"
requires-python = ">=3.9"
authors = [
    { name = "KOGA Mitsuhiro", email = "shiena.jp@gmail.com" }
]
dependencies = [
    "flet>=0.27.1",
]

[project.urls]
Homepage = "https://github.com/shiena"
Documentation = "https://github.com/shiena/dropzone"
Repository = "https://github.com/shiena/dropzone"
Issues = "https://github.com/shiena/dropzone/issues"

[tool.setuptools.package-data]
"flutter.dropzone" = ["**/*"]

[tool.uv]
dev-dependencies = [
    "flet[all]>=0.27.3",
]

[tool.poetry.group.dev.dependencies]
flet = {extras = ["all"], version = "0.27.1"}

[tool.setuptools]
license-files = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
